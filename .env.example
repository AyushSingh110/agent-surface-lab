# Copy to `.env` (gitignored). Ollama-only — no hosted-API secrets (CLAUDE.md 7).
OLLAMA_HOST=http://localhost:11434
# Pinned backbone (work-plan §8 item 8): qwen2.5:7b — reliable multi-step tool-calling.
# Lighter fallback for constrained memory / faster iteration: llama3.2:3b
OLLAMA_MODEL=qwen2.5:7b
# Global seed logged with every run for reproducibility (CLAUDE.md 4).
SEED=20260721
