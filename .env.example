# Copy to `.env` (gitignored). Ollama-only; no hosted-API secrets required.
OLLAMA_HOST=http://localhost:11434
# Pinned backbone: qwen2.5:7b.
# Lighter fallback for constrained memory / faster iteration: llama3.2:3b
OLLAMA_MODEL=qwen2.5:7b
# Global seed logged with every run for reproducibility.
SEED=20260721
