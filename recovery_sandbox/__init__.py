"""Kill-test experiment sandbox for paper-recovery.

Experiment-specific deterministic tools, fixtures, and the task suite for the
recovery kill test. Kept out of `harness/` (which is study-agnostic) but importable
and testable, so ground-truth answers can be verified against fixtures in tests.
"""
